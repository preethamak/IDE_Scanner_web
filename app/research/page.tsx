import Link from "next/link";
import { ArrowRight, BookOpen, FlaskConical, ShieldAlert } from "lucide-react";
import { researchArticles } from "@/lib/research";

export default function ResearchPage() { return <main className="researchIndex"><section className="researchHero"><div><span>IDE Scanner Research</span><h1>Extension security, explained from the evidence.</h1><p>Technical analysis of IDE capabilities, supply-chain behavior, version changes, and the decisions security teams need to defend.</p></div><ShieldAlert/></section><section className="researchFeature"><Link href={`/research/${researchArticles[0].slug}`}><span>{researchArticles[0].category} · {researchArticles[0].published}</span><h2>{researchArticles[0].title}</h2><p>{researchArticles[0].summary}</p><strong>Read the research <ArrowRight/></strong></Link><aside><FlaskConical/><strong>Research standard</strong><p>Claims must link to deterministic scanner evidence, a public source, or a clearly labeled inference. AI summaries cannot create findings.</p><Link href="/benchmark">View validation</Link></aside></section><section className="researchList"><div className="researchListHead"><span>Latest</span><h2>Field notes and methodology</h2></div>{researchArticles.slice(1).map((article) => <Link href={`/research/${article.slug}`} key={article.slug}><BookOpen/><div><span>{article.category} · {article.published} · {article.reading}</span><h3>{article.title}</h3><p>{article.summary}</p></div><ArrowRight/></Link>)}</section></main>; }
