import DesignSystemShowcase from "./DesignSystemShowcase";

export const metadata = { title: "GuardRails interface system." };

export default function DesignSystemPage() {
  return <DesignSystemShowcase />;
}
