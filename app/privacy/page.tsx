import Link from "next/link";
import { ArrowRight, Database, FileArchive, LockKeyhole } from "lucide-react";

export default function PrivacyPage() {
  return <main className="shell trustInfoPage"><header><span>Data handling</span><h1>Know what the service receives and retains.</h1><p>Published artifacts, private package previews, imported reports, and account workspaces have different data boundaries.</p></header><section className="trustInfoGrid"><article><Database/><h2>Published extensions</h2><p>Registry metadata and exact-artifact analysis may be stored as public intelligence, including versions, hashes, normalized findings, dependencies, files, coverage, and scanner identity.</p></article><article><FileArchive/><h2>Private VSIX preview</h2><p>The hosted preview endpoint processes the uploaded package in request memory and returns its result to the browser. The endpoint does not write the uploaded package or preview result to the product database.</p></article><article><LockKeyhole/><h2>Account workspace</h2><p>Authentication protects personal watchlists, scan requests, alert state, notification preferences, and delivery channels. Public artifact reports remain available without sign-in.</p></article></section><section className="trustInfoAction"><div><span>Before uploading</span><h2>Use the correct analysis boundary.</h2><p>For proprietary packages, review the hosted-preview limitations or use a local scanner workflow before sending any artifact to a third-party service.</p></div><Link href="/settings">Read analysis boundaries <ArrowRight/></Link></section></main>;
}
