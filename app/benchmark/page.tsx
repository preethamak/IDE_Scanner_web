import Link from "next/link";
import type { ReactNode } from "react";
import { ArrowUpRight, Database, FileCheck2, FlaskConical, ShieldCheck } from "lucide-react";
import { websiteBenchmark as benchmark } from "@/lib/websiteBenchmark";

const source = (path: string) => `${benchmark.sourceBase}/${path}`;

export default function BenchmarkPage() {
  return <main className="studyPage pageWrap">
    <section className="studyHero"><div><span className="kicker">Frozen benchmark · {benchmark.publishedAt}</span><h1>Regression evidence with its limits intact.</h1><p>30 exact Visual Studio Marketplace artifacts were frozen and hash-pinned before analysis. This study documents scanner improvements; it is not an ecosystem-wide accuracy or malware-recall claim.</p></div><div className="studyStamp"><FileCheck2 size={19}/><span>Final scanner commit</span><strong><code>{benchmark.scannerCommit.slice(0, 12)}</code></strong><span>Artifacts</span><strong>{benchmark.corpus.artifacts} VSIX files</strong></div></section>

    <section className="studyNumbers"><article><strong>{benchmark.untouched.completed}</strong><span>untouched artifacts completed</span></article><article><strong>{benchmark.untouched.routed}</strong><span>untouched completed artifacts routed as expected</span></article><article><strong>{benchmark.regression.completed}</strong><span>development-regression artifacts completed</span></article><article><strong>0</strong><span>legitimate blocks after documented corrections</span></article></section>

    <section className="studyInterpretation"><div><span className="kicker">Correct interpretation</span><h2>Useful regression evidence, not an accuracy score.</h2></div><div><p>The untouched first pass exposed 1 false block, 1 missed credential-related review route, 1 benchmark-label error, and 7 coverage gaps. Those defects were retained and documented before the scanner changed.</p><p>After the documented corrections, all 30 artifacts completed. {benchmark.regression.decisions.allow} narrow or declarative artifacts were allowed and {benchmark.regression.decisions.review} privileged artifacts were routed to review; no legitimate artifact was blocked.</p></div></section>

    <section className="studySection"><div className="resultHeader"><div><span className="kicker">Untouched first pass</span><h2>What the original run actually showed</h2></div><span>Wilson 95% intervals shown</span></div><div className="methodNote"><Metric icon={<Database/>} title="Coverage" value={benchmark.untouched.completed} detail={benchmark.untouched.completedInterval}/><Metric icon={<ShieldCheck/>} title="Predeclared routing" value={benchmark.untouched.routed} detail={benchmark.untouched.routedInterval}/><Metric icon={<FlaskConical/>} title="Legitimate false blocks" value={benchmark.untouched.legitimateBlocks} detail={benchmark.untouched.legitimateBlocksInterval}/></div></section>

    <section className="studySection"><div className="resultHeader"><div><span className="kicker">Final development regression</span><h2>Stable after the documented fixes</h2></div><span>Not an independent holdout</span></div><div className="methodNote"><Metric icon={<Database/>} title="Coverage" value={benchmark.regression.completed} detail={benchmark.regression.completedInterval}/><Metric icon={<ShieldCheck/>} title="Artifact-aware routing" value={benchmark.regression.routed} detail={benchmark.regression.routedInterval}/><Metric icon={<FlaskConical/>} title="Legitimate false blocks" value={benchmark.regression.legitimateBlocks} detail={benchmark.regression.legitimateBlocksInterval}/></div></section>

    <section className="studySection recurringSection"><div className="sectionTitle"><span className="kicker">Scope boundary</span><h2>Claims this benchmark does not support.</h2><p>The corpus informed scanner improvements and contains no fresh malicious holdout. The retained malicious sample is a development regression, not a recall denominator.</p></div><div className="recurringList"><article><span>01</span><strong>No ecosystem-wide accuracy claim</strong><div><i style={{ width: "0%" }}/></div><b>Not claimed</b></article><article><span>02</span><strong>No malware recall or false-negative rate</strong><div><i style={{ width: "0%" }}/></div><b>Not claimed</b></article><article><span>03</span><strong>No claim that an allowed artifact is proven safe</strong><div><i style={{ width: "0%" }}/></div><b>Not claimed</b></article></div></section>

    <section className="studyCta"><div><span className="kicker">Reproduce & inspect</span><h2>Read the inputs, results, corrections, and integrity hashes.</h2><p>The retained malicious development sample, <code>{benchmark.maliciousDevelopmentRegression.artifact}</code>, remained blocked with malware {benchmark.maliciousDevelopmentRegression.malwareScore} and risk {benchmark.maliciousDevelopmentRegression.riskScore}.</p></div><div className="heroActions"><a className="button buttonDark" href={source("reports/website-benchmark-2026-07-16.md")}>Read full report <ArrowUpRight size={16}/></a><a className="button buttonQuiet" href={source("benchmarks/website-corpus/v1/manifest.json")}>Artifact manifest <ArrowUpRight size={16}/></a><a className="button buttonQuiet" href={source("benchmarks/website-corpus/v1/results.json")}>Normalized results <ArrowUpRight size={16}/></a><Link className="button buttonQuiet" href="/scoring">Decision methodology</Link></div></section>
  </main>;
}

function Metric({ icon, title, value, detail }: { icon: ReactNode; title: string; value: string; detail: string }) {
  return <article>{icon}<h3>{title}</h3><p><strong>{value}</strong><br/>{detail}</p></article>;
}
