/** Immutable corpus facts. Scan outcomes are resolved from canonical reports. */
export const websiteBenchmark = {
  corpus: {
    artifacts: 30,
    bytes: 697_056_453,
    freshHoldouts: 21,
    priorExposureControls: 9,
  },
} as const;
